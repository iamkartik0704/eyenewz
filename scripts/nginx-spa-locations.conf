# SPA article routes — fall back to the React index.html.
# Include from the eyenewz.com server block, before `location /`.
# Live file: /etc/nginx/sites-available/eyenewz

location /a/ {
    try_files $uri $uri/ /index.html;
}

location /article/ {
    try_files $uri $uri/ /index.html;
}
